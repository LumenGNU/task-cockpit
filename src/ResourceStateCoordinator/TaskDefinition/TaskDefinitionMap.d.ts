import type TaskDefinitionEntry from './TaskDefinitionEntry';
import type TaskName from '../../TaskName';

type TaskDefinitionMap = Map<TaskName, TaskDefinitionEntry>;

export default TaskDefinitionMap;
