{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "existin task",
            "type": "shell",
            "command": "echo 'existin task'",
            "problemMatcher": []
        },
        {
            "label": "depends on 'existin task'",
            "dependsOn": "existin task", // существует
            "problemMatcher": []
        },
        {
            "label": "depends on 'non existin task'",
            "dependsOn": "non existin task", // НЕ существует
            "problemMatcher": []
        },
        {
            "label": "depends on 'non-existin script' (1)",
            // Скрипт не существует
            // НО диагностика НЕ работает с такой записью
            "dependsOn": { "type": "npm", "script": "install" },
            "problemMatcher": []
        },
        {
            "label": "depends on 'non-existin script' (2)",
            // Скрипт не существует
            // НО диагностика НЕ работает с такой записью
            "dependsOn": [
                { "type": "npm", "script": "install" }
            ],
            "problemMatcher": []
        }
    ]
}
