{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "existing task",
            "type": "shell",
            "command": "echo 'existing task'",
            "problemMatcher": []
        },
        {
            "label": "depends on 'existing task'",
            "dependsOn": "existing task", // существует
            "problemMatcher": []
        },
        {
            "label": "depends on 'non existing task'",
            "dependsOn": "non existing task", // НЕ существует
            "problemMatcher": []
        },
        {
            "label": "depends on 'non-existingscript' (1)",
            // Скрипт не существует
            // НО диагностика НЕ работает с такой записью
            "dependsOn": { "type": "npm", "script": "install" },
            "problemMatcher": []
        },
        {
            "label": "depends on 'non-existingscript' (2)",
            // Скрипт не существует
            // НО диагностика НЕ работает с такой записью
            "dependsOn": [
                { "type": "npm", "script": "install" }
            ],
            "problemMatcher": []
        }
    ]
}
